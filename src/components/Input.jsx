// MUST start with a capital letter
// generally a good idea for the file and the component name to be the same
const Input = () => (
    <label>My Label
        <input type="text" />
    </label>
);

export default Input;